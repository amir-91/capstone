describe("Testing getCityData functionality", () => {
    test("Testing the getData() function", () => {
        const city = ''
        const postalCode = ''
        const date = ''
        expect(city).not.toBeNull();
        expect(postalCode).not.toBeNull();
        expect(date).not.toBeNull();
})})